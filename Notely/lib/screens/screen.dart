import 'package:flutter/material.dart';

class Screen {
  @override
  Widget build (BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Screen'),
        elevation: 1,
        ),
      );
  }
}